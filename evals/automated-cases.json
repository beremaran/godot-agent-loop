{
  "schemaVersion": 1,
  "scenarioSetVersion": 1,
  "evaluationMode": "automated",
  "cases": [
    {
      "id": "real-mcp-compatibility",
      "status": "executable",
      "command": "npx vitest run --config vitest.e2e.config.ts tests/e2e/agent-adapter-smoke.test.ts",
      "requirements": ["built server", "supported Godot 4.7 binary"],
      "covers": ["catalog filters", "catalog and call", "Pi forwarding", "teardown"],
      "doesNotCover": ["cold-model selection", "watched human-control scenarios"]
    }
  ]
}
