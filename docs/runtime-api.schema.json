{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/beremaran/godot-mcp/runtime-api-1.0.schema.json",
  "title": "Godot MCP Runtime API",
  "description": "Version 1.0 JSON-RPC 2.0 API. Messages are one JSON value per newline over TCP.",
  "type": "object",
  "oneOf": [{ "$ref": "#/$defs/request" }, { "$ref": "#/$defs/successResponse" }, { "$ref": "#/$defs/errorResponse" }],
  "$defs": {
    "id": { "type": ["string", "integer"] },
    "godotValue": { "description": "JSON-safe representation produced by the runtime Variant codec. Null, bool, int, float, and String retain their JSON representation; Vector2/2i/3/3i, Color, Quaternion, Basis, Transform2D/3D, Rect2, and AABB use component objects; NodePath and StringName use strings; packed and ordinary collections recurse to JSON arrays/objects; Node, Resource, and Object use tagged {_type, class, ...} objects. Unsupported Variant types, cycles, excessive depth, and excessive collection size are command failures rather than string fallbacks. JSON numbers follow Godot/JSON double precision, so integers outside the interoperable ±(2^53-1) range may lose precision in JavaScript clients.", "type": ["null", "boolean", "number", "string", "array", "object"] },
    "request": { "type": "object", "required": ["jsonrpc", "id", "method"], "properties": { "jsonrpc": { "const": "2.0" }, "id": { "$ref": "#/$defs/id" }, "method": { "type": "string", "pattern": "^godot\\.runtime\\.(handshake|[a-z0-9_]+)$" }, "params": { "type": "object" } }, "additionalProperties": false },
    "successResponse": { "type": "object", "required": ["jsonrpc", "id", "result"], "properties": { "jsonrpc": { "const": "2.0" }, "id": { "$ref": "#/$defs/id" }, "result": { "$ref": "#/$defs/godotValue" } }, "additionalProperties": false },
    "errorResponse": { "type": "object", "required": ["jsonrpc", "id", "error"], "properties": { "jsonrpc": { "const": "2.0" }, "id": { "oneOf": [{ "$ref": "#/$defs/id" }, { "type": "null" }] }, "error": { "type": "object", "required": ["code", "message"], "properties": { "code": { "type": "integer" }, "message": { "type": "string" }, "data": { "$ref": "#/$defs/godotValue" } }, "additionalProperties": false } }, "additionalProperties": false }
  },
  "x-runtime-contract": {
    "protocolVersion": "1.0", "capabilities": ["runtime-commands", "godot-json-values"],
    "authoring": { "capability": "authoring-commands", "commands": ["authoring_add_node", "authoring_attach_script", "authoring_create_resource", "authoring_create_scene", "authoring_export_mesh_library", "authoring_get_uid", "authoring_load_sprite", "authoring_manage_resource", "authoring_manage_scene_signals", "authoring_manage_scene_structure", "authoring_manage_theme_resource", "authoring_modify_node", "authoring_read_scene", "authoring_remove_node", "authoring_resave_resources", "authoring_save_scene"], "semantics": "Authoring commands share the authenticated newline-delimited JSON-RPC transport but are advertised only when godot_operations.gd owns the process main loop and registers its dispatcher. Their authoring_ namespace avoids collisions with live-scene commands such as remove_node. During Phase 6c, ToolBackend routes operations to this surface one at a time while the subprocess backend remains the fallback." },
    "renderingContext": { "capability": "rendering-context", "requiredFor": ["authoring session", "screenshot", "visual regression"], "unavailableReason": "rendering_context_unavailable", "semantics": "The capability is advertised only when Godot uses a headed display driver. The persistent authoring harness requires it during startup and terminates immediately with display/Xvfb remediation when unavailable. Screenshot checks the same precondition before awaiting a process frame or reading viewport texture data, so a display-less run fails rather than hanging." },
    "editorSync": { "bridgeCommand": "filesystem_changed", "trigger": "successful mutating authoring-session response", "readOnlyCommandsExcluded": ["authoring_get_uid", "authoring_read_scene", "manage_resource read", "manage_theme_resource read", "manage_scene_signals list"], "focusField": "focus_path", "semantics": "The server normalizes affected scene/resource paths to res:// and derives a scene-relative focus_path when the operation identifies a node. The addon calls EditorFileSystem.scan(), reload_scene_from_path only when the changed scene is currently edited, then selects the focus node and calls EditorInterface.edit_node to reveal it. Explicit editor_control select uses the same reveal behavior. Editor absence or sync failure never changes the authoring command result." },
    "concurrentEditing": { "bridgeCommand": "driver_state", "dock": "Agent Activity", "defaultState": "agent_driving", "pauseControl": "Pause Agent", "resumeControl": "Resume Agent", "absentEditor": "allow", "pausedMutation": "refuse_before_dispatch", "classification": "deny-by-default: every tool call is mutating unless its tool or selected action is explicitly allowlisted as observational", "semantics": "Before a mutating tool handler runs, the server queries the authenticated editor addon. Pause Agent makes subsequent mutations return an actionable MCP tool error while observational calls remain available; Resume Agent restores mutation. If no editor bridge is reachable, unattended operation continues. The cooperative state is checked before each dispatch and does not cancel a mutation already in flight." },
    "authentication": { "capability": "session-authentication", "handshakeParam": "secret", "environment": "GODOT_MCP_RUNTIME_SECRET", "generatedBits": 256, "errorCode": -32008, "semantics": "An MCP-owned launch generates a fresh secret and passes it only in the Godot child environment. Every TCP session must authenticate in its handshake before any command or cancellation request is accepted. Authentication failures and pre-authentication denials never echo request parameters or secret values. A user-managed runtime with no configured secret retains legacy unauthenticated handshake behavior and does not advertise this capability." },
    "privilegedCommandPolicy": { "capability": "privileged-commands", "groups": { "reflection": ["call_method", "get_property", "set_property"], "code-execution": ["eval", "script"], "network": ["http_request", "rpc", "websocket"] }, "commands": ["call_method", "eval", "get_property", "http_request", "rpc", "script", "set_property", "websocket"], "default": "deny", "enable": { "groupsEnvironment": "GODOT_MCP_PRIVILEGED_GROUPS=reflection,code-execution,network", "allEnvironment": "GODOT_MCP_ALLOW_PRIVILEGED_COMMANDS=true", "godotExport": "allow_privileged_commands=true" }, "trustBoundary": "The server binds to 127.0.0.1 and MCP-owned launches require a per-session secret. Reflection, code execution, and outbound/peer networking are independently negotiated and denied by default; the legacy all-command opt-in remains available for trusted development.", "deniedErrorCode": -32007, "deniedError": "Denied responses contain only policy metadata including the required group; request params, source, property values, URLs, headers, and engine error text are not copied into the denial." },
    "commands": ["3d_effects", "add_collision", "animation_control", "animation_tree", "audio_bus", "audio_bus_layout", "audio_effect", "audio_play", "audio_spatial", "authoring_add_node", "authoring_attach_script", "authoring_create_resource", "authoring_create_scene", "authoring_export_mesh_library", "authoring_get_uid", "authoring_load_sprite", "authoring_manage_resource", "authoring_manage_scene_signals", "authoring_manage_scene_structure", "authoring_manage_theme_resource", "authoring_modify_node", "authoring_read_scene", "authoring_remove_node", "authoring_resave_resources", "authoring_save_scene", "await_signal", "bone_pose", "call_method", "camera_attributes", "canvas", "canvas_draw", "change_scene", "click", "connect_signal", "create_animation", "create_joint", "create_timer", "csg", "debug_draw", "disconnect_signal", "emit_signal", "environment", "eval", "find_nodes_by_class", "gamepad", "get_audio", "get_camera", "get_node_info", "get_nodes_in_group", "get_performance", "get_property", "get_scene_tree", "get_ui_elements", "gi", "gridmap", "http_request", "input_action", "input_state", "instantiate_scene", "key_hold", "key_press", "key_release", "light_2d", "light_3d", "list_signals", "locale", "manage_group", "mesh_instance", "mouse_drag", "mouse_move", "multimesh", "multiplayer", "navigate_path", "navigation_3d", "os_info", "parallax", "path_2d", "path_3d", "pause", "physics_2d", "physics_3d", "physics_body", "play_animation", "procedural_mesh", "process_mode", "raycast", "remove_node", "render_settings", "reparent_node", "resource", "rpc", "screenshot", "script", "scroll", "serialize_state", "set_camera", "set_particles", "set_property", "set_shader_param", "shape_2d", "skeleton_ik", "sky", "spawn_node", "terrain", "tilemap", "time_scale", "touch", "tween_property", "ui_control", "ui_item_list", "ui_menu", "ui_popup", "ui_range", "ui_tabs", "ui_text", "ui_theme", "ui_tree", "video", "viewport", "visual_shader", "wait", "websocket", "window", "world_settings"],
    "routing": "commands is the authoritative manifest: the GDScript registry and the TypeScript RUNTIME_COMMANDS binding must match it exactly, and the contract test rejects missing, extra, or misnamed entries on either side. Commands dispatch through a typed registry. An unknown method, including an unregistered godot.runtime.* command, receives -32601 before any busy or lifecycle state is touched.",
    "concurrency": "One command is processed at a time. A second command, including one from another session, receives -32001 and is not queued. Handshake and cancel are control requests and may be processed while a command is running.",
    "determinism": { "fixedFps": 60, "initialTimeScale": 1, "wallClockFpsCap": 60, "metadataEnvironment": "GODOT_MCP_FIXED_FPS", "controlCommand": "time_scale", "semantics": "Every MCP-owned long-running Godot process starts with --fixed-fps 60, --max-fps 60, and --time-scale 1. Each rendered frame therefore advances a stable simulation delta without allowing an idle session to spin unbounded. The harness mirrors the fixed value into GODOT_MCP_FIXED_FPS because Godot strips recognized engine flags from script-visible arguments. time_scale get/set provides explicit runtime control and reports the configured fixed_fps; callers restore time_scale=1 when a scenario finishes." },
    "lifecycle": "A command transitions received → running → responded, cancelled, or timed_out. Terminal states are exclusive: exactly one response is emitted for a connected session; a disconnected session's terminal response is discarded.",
    "observability": { "correlationParam": "_mcp_correlation_id", "components": ["godot-mcp-server", "godot-mcp-runtime"], "events": ["request_started", "request_finished", "request_completed", "request_failed", "cancellation_requested", "request_cancelled", "request_timed_out", "request_abandoned", "authentication_succeeded", "authentication_failed", "authorization_denied"], "redaction": "Logs contain controlled method/command names, internal correlation and session IDs, lifecycle state, duration, and stable error codes only. Request parameters, response values, secrets, source, URLs, and raw parse failures are never logged. The local authenticated editor dock additionally receives one bounded target string selected from documented path/group/action fields; it is never copied to logs.", "retention": "GodotProcessManager retains at most 1000 recent stdout and stderr lines per active process; external stderr consumers control MCP server log retention.", "editorSink": { "bridgeCommand": "activity", "dock": "Agent Activity", "fields": ["correlation_id", "command", "target", "outcome", "duration_ms"], "maxEntries": 200, "semantics": "Running-game and authoring-session GameConnection events are pushed best-effort over the authenticated editor bridge. Editor availability never blocks or changes command delivery." } },
    "cancellation": { "method": "godot.runtime.cancel", "params": { "request_id": "The JSON-RPC id of the caller's active request." }, "cancellableCommands": ["wait", "await_signal", "resource", "http_request"], "semantics": "Cancellation is cooperative. cancel returns an acknowledgement, then the original request terminates with -32003. wait and await_signal check between frames; threaded resource preloads and HTTP requests check while awaiting completion and release their owned work on cancellation. Other commands return -32005 and continue running." },
    "timeout": "await_signal has a runtime deadline from its timeout parameter and terminates with -32004. Client-side timeouts are local and issue a best-effort cancel request; non-cancellable commands continue until their normal terminal response or session disconnect.",
    "limits": { "requestLineBytes": 1048576, "receiveBufferBytes": 2097152, "receiveChunkBytes": 65536, "jsonNestingDepth": 32, "jsonCollectionItems": 1024, "responseBytes": 8388608, "screenshotPixels": 16777216, "screenshotPngBytes": 6291456, "sceneTreeNodesDefault": 1000, "sceneTreeNodesMaximum": 10000, "processLogLines": 1000, "logPageItems": 1000, "headlessBufferBytes": 16777216, "semantics": "The runtime exposes transport defaults as configurable server exports. Scene trees use deterministic pre-order truncation; process logs use lossless bounded pages over bounded recent retention; file lists use deterministic cursor pagination. An oversized or invalid-UTF-8 request frame, or a request whose decoded JSON exceeds nesting or collection limits, receives -32006 and only that session is closed. Runtime resources and other responses larger than responseBytes are replaced with -32006; headless/import output is bounded by headlessBufferBytes; screenshots are rejected before base64 encoding when their dimensions or PNG payload exceed their limits." },
    "variantCodec": { "typeHints": ["", "String", "Vector2", "Vector2i", "Vector3", "Vector3i", "Color", "Quaternion", "Rect2", "AABB", "Basis", "Transform3D", "Transform2D"], "propertyAwareDecoding": "An empty type_hint means no explicit type; scalar values and tagged objects remain JSON-compatible, while component-key dictionaries use documented inference. set_property, tween_property, and scene-state restoration use Godot property metadata for scalar and math-type conversion; other dictionary inputs use an explicit type_hint or documented component-key inference.", "objects": "Nodes encode as {_type:'Node', class, name, path}; Resources as {_type:'Resource', class, path}; other Objects as {_type:'Object', class, id}. Tagged objects are diagnostic representations and are not decoded into live engine objects.", "limitsAndErrors": "Incoming JSON is bounded by jsonNestingDepth/jsonCollectionItems before codec decoding; recursive Variant encoding applies the same limits. Cycles, unsupported Variant types, malformed/unknown type hints, and codec limit violations terminate the command with -32000 and structured error.data.reason: codec_cycle, unsupported_variant, invalid_type_hint, codec_depth_exceeded, or codec_collection_exceeded." },
    "errorCodes": { "-32700": "Parse error", "-32600": "Invalid request", "-32601": "Method not found", "-32602": "Invalid params", "-32001": "Server busy", "-32002": "Protocol version mismatch", "-32003": "Request cancelled", "-32004": "Request timed out or is not running", "-32005": "Request is not cancellable", "-32006": "Configured runtime transport limit exceeded", "-32007": "Privileged command disabled by policy", "-32008": "Runtime session authentication failed or required", "-32000": "Runtime command failed" },
    "commandFailureData": "A -32000 command failure may carry a structured error.data object. Parameter-validation and lookup failures set {\"param\": name, \"reason\": one of missing | invalid_type | invalid_value | out_of_range | node_not_found | method_not_found | resource_not_found, ...context}; enum failures add \"allowed\", range failures add \"min\"/\"max\"/\"value\". Failures caused by a Godot Error value set {\"reason\": \"godot_error\", \"godot_error\": code, \"godot_error_string\": text}. A -32007 policy denial sets reason=privileged_command_disabled and never includes request parameters. error.data is additive: clients must key behavior on error.code and may use error.data for diagnostics."
  }
}
